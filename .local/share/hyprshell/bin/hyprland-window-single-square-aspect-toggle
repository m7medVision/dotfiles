#!/bin/bash

# omarchy:summary=Toggle single-window square aspect ratio.

case $(hyprland-toggle single-window-aspect-ratio) in
  on) notification-send -g  "Enable single-window square aspect ratio" ;;
  off) notification-send -g  "Disable single-window square aspect ratio" ;;
esac
