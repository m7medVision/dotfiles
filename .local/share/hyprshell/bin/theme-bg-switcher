#!/bin/bash

# omarchy:summary=Open the Omarchy background switcher
# omarchy:group=theme
# omarchy:name=bg-switcher
# omarchy:aliases=omarchy background

theme_name=$(cat "$HOME/.local/state/hyprshell/current/theme.name" 2>/dev/null)
current_background=$(readlink -f "$HOME/.local/state/hyprshell/current/background" 2>/dev/null)

menu-images \
  --selected "$current_background" \
  "$HOME/.local/state/hyprshell/current/theme/backgrounds" \
  "$HOME/.config/hyprshell/backgrounds/$theme_name"
